<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Painel da Guilda</title>
    <style>
        body { background: #2c2f33; color: white; font-family: sans-serif; margin: 0; }
        nav { background: #23272a; padding: 20px; display: flex; justify-content: space-between; border-bottom: 2px solid #5865F2; }
        .container { padding: 30px; }
        table { width: 100%; background: #23272a; border-radius: 8px; border-collapse: collapse; }
        th, td { padding: 15px; text-align: left; border-bottom: 1px solid #2f3136; }
        .membro { display: flex; align-items: center; gap: 10px; }
        .membro img { width: 35px; border-radius: 50%; }
        .status-on { color: #43b581; font-weight: bold; }
        .status-off { color: #f04747; font-weight: bold; }
    </style>
</head>
<body>
    <nav>
        <span>⚔️ PAINEL DE MEMBROS</span>
        <a href="/settings" style="color: white; text-decoration: none; font-weight: bold;">⚙️ CONFIGURAÇÕES</a>
    </nav>
    <div class="container">
        <table>
            <thead>
                <tr>
                    <th>Membro Discord</th>
                    <th>Nick FF</th>
                    <th>UID FF</th>
                    <th>Atividade</th>
                </tr>
            </thead>
            <tbody>
                <% members.forEach(m => { %>
                <tr>
                    <td class="membro">
                        <img src="<%= m.avatar || 'https://cdn.discordapp.com/embed/avatars/0.png' %>">
                        <%= m.name %>
                    </td>
                    <td style="color:#ffcc00"><%= m.ffNick %></td>
                    <td style="font-family: monospace;"><%= m.ffId %></td>
                    <td>
                        <% if (Date.now() - m.lastMessage > 604800000) { %>
                            <span class="status-off">🔴 INATIVO</span>
                        <% } else { %>
                            <span class="status-on">🟢 ATIVO</span>
                        <% } %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
