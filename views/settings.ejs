<!DOCTYPE html>
<html>
<head>
    <title>Configurações do Bot</title>
    <style>
        body { background: #121212; color: white; font-family: 'Segoe UI', sans-serif; margin: 0; }
        nav { background: #1e1e1e; padding: 20px; display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #5865F2; }
        .container { padding: 40px; max-width: 600px; margin: auto; }
        .card { background: #1e1e1e; padding: 30px; border-radius: 10px; }
        select, input { width: 100%; padding: 12px; margin: 10px 0; background: #2f3136; color: white; border: 1px solid #444; border-radius: 5px; }
        button { background: #43b581; color: white; width: 100%; padding: 15px; border: none; border-radius: 5px; cursor: pointer; font-weight: bold; font-size: 16px; margin-top: 20px; }
        .btn-back { color: #aaa; text-decoration: none; font-size: 14px; }
        .success-msg { background: #43b581; color: white; padding: 10px; border-radius: 5px; margin-bottom: 20px; text-align: center; }
    </style>
</head>
<body>
    <nav>
        <span>⚙️ CONFIGURAÇÕES</span>
        <a href="/dashboard" style="color: white; text-decoration: none;">📊 VER MEMBROS</a>
    </nav>

    <div class="container">
        <div class="card">
            <% if (typeof success !== 'undefined' || false) { %>
                <div class="success-msg">✅ Configurações salvas com sucesso!</div>
            <% } %>

            <form action="/save" method="POST">
                <label>Remover cargo ao verificar (ex: Novato):</label>
                <select name="roleNovato">
                    <option value="">-- Nenhum --</option>
                    <% roles.forEach(r => { %>
                        <option value="<%= r.id %>" <%= config.roleNovato == r.id ? 'selected' : '' %>><%= r.name %></option>
                    <% }) %>
                </select>

                <label>Dar este cargo 1:</label>
                <select name="roleVerificado1">
                    <option value="">-- Nenhum --</option>
                    <% roles.forEach(r => { %>
                        <option value="<%= r.id %>" <%= config.roleVerificado1 == r.id ? 'selected' : '' %>><%= r.name %></option>
                    <% }) %>
                </select>

                <label>Dar este cargo 2:</label>
                <select name="roleVerificado2">
                    <option value="">-- Nenhum --</option>
                    <% roles.forEach(r => { %>
                        <option value="<%= r.id %>" <%= config.roleVerificado2 == r.id ? 'selected' : '' %>><%= r.name %></option>
                    <% }) %>
                </select>

                <label>Canal para o aviso de Sábado:</label>
                <select name="canalAviso">
                    <option value="">-- Nenhum --</option>
                    <% channels.forEach(c => { %>
                        <option value="<%= c.id %>" <%= config.canalAviso == c.id ? 'selected' : '' %>><%= c.name %></option>
                    <% }) %>
                </select>

                <label>Mensagem da Guerra:</label>
                <input type="text" name="msgGuerra" value="<%= config.msgGuerra || '@everyone Guerra de Guilda!' %>">

                <button type="submit">SALVAR CONFIGURAÇÕES</button>
            </form>
            <br>
            <center><a href="/dashboard" class="btn-back">Cancelar e Voltar</a></center>
        </div>
    </div>
</body>
</html>
