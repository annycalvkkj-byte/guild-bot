<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Configurações</title>
    <style>
        body { background: #2c2f33; color: white; font-family: sans-serif; padding: 20px; }
        .card { background: #23272a; padding: 25px; border-radius: 10px; max-width: 600px; margin: auto; }
        select, input { width: 100%; padding: 10px; margin: 10px 0; background: #1a1c1e; border: 1px solid #444; color: white; }
        .btn-save { background: #43b581; color: white; border: none; padding: 15px; width: 100%; cursor: pointer; font-weight: bold; }
        .btn-setup { background: #5865F2; color: white; border: none; padding: 15px; width: 100%; cursor: pointer; font-weight: bold; margin-top: 20px; }
    </style>
</head>
<body>
    <div class="card">
        <h1>Configurações da Guilda</h1>
        <form action="/save" method="POST">
            <label>Cargo de Novato (Remover):</label>
            <select name="roleNovato">
                <option value="">Nenhum</option>
                <% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.roleNovato == r.id ? 'selected' : '' %>><%= r.name %></option><% }) %>
            </select>

            <label>Cargos para Dar (1 e 2):</label>
            <select name="roleVerificado1">
                <option value="">Nenhum</option>
                <% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.roleVerificado1 == r.id ? 'selected' : '' %>><%= r.name %></option><% }) %>
            </select>
            <select name="roleVerificado2">
                <option value="">Nenhum</option>
                <% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.roleVerificado2 == r.id ? 'selected' : '' %>><%= r.name %></option><% }) %>
            </select>

            <label>Canal onde a Verificação será enviada:</label>
            <select name="canalVerificacao">
                <option value="">Selecione o canal...</option>
                <% channels.forEach(c => { %><option value="<%= c.id %>" <%= config.canalVerificacao == c.id ? 'selected' : '' %>><%= c.name %></option><% }) %>
            </select>

            <label>Canal de Aviso Guerra (Sábado):</label>
            <select name="canalAviso">
                <option value="">Nenhum</option>
                <% channels.forEach(c => { %><option value="<%= c.id %>" <%= config.canalAviso == c.id ? 'selected' : '' %>><%= c.name %></option><% }) %>
            </select>

            <button type="submit" class="btn-save">SALVAR TODAS AS OPÇÕES</button>
        </form>

        <hr style="margin: 30px 0; border: 1px solid #444;">
        
        <h3>Enviar Mensagem de Registro</h3>
        <p style="font-size: 12px; color: #aaa;">Escolha o "Canal de Verificação" acima, salve, e depois clique no botão abaixo:</p>
        <form action="/send-setup" method="POST">
            <button type="submit" class="btn-setup">ENVIAR BOTÃO DE REGISTRO PARA O DISCORD</button>
        </form>
        <br>
        <center><a href="/dashboard" style="color:#aaa">Voltar para Membros</a></center>
    </div>
</body>
</html>
